# Application environment variables example
# Copy this to .env and customize as needed

# Mode: development or production
APP_ENV=development

# Log level config (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# Sourcing optimization weights (cost vs speed ratio: 0.0 - 1.0)
COST_WEIGHT=0.5
SPEED_WEIGHT=0.5
